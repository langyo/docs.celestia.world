# Entelecheia — агентная платформа и память

Entelecheia — это агентная платформа: рантайм scepter, который оркестрирует
специализированных агентов («души»), поддерживает долгосрочную память
(PhiLia), обеспечивает семантический поиск и размещает слой промышленной
интеграции. За возможностями Arona и Chest стоит именно эта платформа. Это
руководство организовано по возможностям: агенты, память, поиск, знания и
подключения.

## Архитектура одним взглядом

```text
Клиенты: Arona (шлюз) · Shittim Chest (чат/панели) · TUI/CLI
        │  JSON-RPC 2.0 поверх WebSocket (токен или API key)
        ▼
   рантайм scepter (node-3:8424)
   ├─ Менеджер агентов: L1-души (PhiLia, Skopeo, Hubris, Kalos, …)
   ├─ Цепочки навыков: конвейеры вызовов LLM + инструментов с RAG-префетчем
   ├─ PhiLia: долгосрочная память (векторы + граф, на pgvector)
   ├─ ApoRia: база знаний + индекс рабочих пространств (семантический поиск)
   ├─ OreXis: шлюзы политик и безопасности для исполнения инструментов
   └─ Рефлексия: хранилище уроков, повторно внедряемое в промпты
```

## 1. Агенты (души)

Каждая душа — специализированный агент с собственным документом личности,
инструментами (в стиле MCP) и навыками. Цепочки навыков компонуют вызовы LLM
с исполнением инструментов; перед каждым вызовом релевантные долгосрочные
воспоминания и содержимое базы знаний предварительно извлекаются и
внедряются в системный промпт.

Безопасность: исполнение инструментов проходит через шлюзы политик OreXis, а
промышленные записи требуют явных потоков подтверждения.

## 2. Долгосрочная память (PhiLia)

PhiLia — это сервис памяти за шлюзом памяти Arona:

- **Хранение** — эпизоды, сущности и артефакты хранятся как узлы в графе
  памяти, векторизуются и отражаются в pgvector (`philia_chunks`).
- **Запрос** — семантическое извлечение объединяет векторное сходство, обход
  графа и затухание по свежести (период полураспада 14 дней).
- **Консолидация** — периодические слияния связывают родственные узлы.
- **Сетевая поверхность** — первоклассные методы `Sync.MemoryStoreRequest` /
  `MemoryQueryRequest` / `MemoryDeleteRequest` (RBAC: SystemWrite /
  SystemRead) рядом с генерическим маршрутом `Mcp.CallTool`.

Векторизация: настраивается через `OLLAMA_HOST` + `OLLAMA_EMBED_MODEL`
(например `nomic-embed-text`) или через удалённый API с откатом к локальной
ONNX-модели.

## 3. Семантический поиск

`Sync.SearchRequest` сливает два хранилища в один ранжированный список:

- **ApoRia** — индекс рабочих пространств, отчёты агентов и документы баз
  знаний (гибридный векторно-ключевой поиск с RRF).
- **PhiLia** — долгосрочные воспоминания (источник `philia_memory`).

## 4. База знаний

Создавайте базы знаний, добавляйте документы и оформляйте
RAG-подписки — всё хранится в Postgres. Документы векторизуются в хранилище
ApoRia и извлекаются через ту же поисковую поверхность.

## 5. Рефлексия

Извлечённые уроки хранятся в хранилище уроков (pgvector) и повторно
внедряются в будущие промпты — вторая, лёгкая постоянная память рядом с
PhiLia.

## 6. Подключение клиентов

- WebSocket `ws://<host>:8424/ws` — аутентификация при апгрейде через
  `?token=<connection token>` (или Bearer); затем `Sync.ConnectHandshake`.
- HTTP JSON-RPC `POST /api/rpc?token=…` для сценариев запрос/ответ.
- Токен подключения: `~/.config/entelecheia/scepter.token` на узле scepter.

## Справочник переменных окружения (подмножество)

| Переменная | Назначение |
|---|---|
| `SERVER_BIND_ADDRESS` | Адрес привязки (по умолчанию 127.0.0.1; задайте 0.0.0.0:8424 для удалённых клиентов) |
| `DATABASE_URL` | Postgres (config.toml или env) |
| `OLLAMA_HOST` / `OLLAMA_EMBED_MODEL` | Бэкенд векторизации |
| `JWT_SECRET` | Постоянные токены аутентификации (случайные на каждую сессию, если не задан) |
| `connection_token` | Файл токена подключения scepter |
