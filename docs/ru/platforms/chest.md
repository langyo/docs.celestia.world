# Shittim Chest — чат, панели и интеграции

Shittim Chest — это лицо пользователя: чат, панели и мультиканальные
интеграции — слой, превращающий возможности моделей и агентов в то, чем вы
пользуетесь каждый день. Он берёт модели и память у Arona, а агентов, панели
и промышленные рабочие процессы — у scepter из entelecheia. Это руководство
организовано по возможностям: чат, панели, каналы, поиск и аутентификация.

## Архитектура одним взглядом

```text
Вы
 │  desktop-приложение · web UI · 12+ IM-каналов (discord/slack/matrix/lark/…)
 ▼
Ядро Shittim Chest (node-2:8425)
 ├─ Чат: диалоги хранятся в Postgres, стриминг SSE/WS
 ├─ Панели: таблица данных / медиа-конвейер / 3D-двойник (движок панелей)
 ├─ Медиа-конвейер: проверка LLM-зрением, генерация изображений, 3D-ген (заглушка)
 ├─ Мост каналов: двунаправленный IM ↔ ядро (webhooks + WS)
 └─ RPC-мост → scepter (агенты, семантический поиск, база знаний)
        │
        ▼
Arona (модели, память) · scepter (агенты, панели, поиск)
```

## 1. Чат

- Диалоги и сообщения хранятся в Postgres (таблицы `conversations` /
  `messages`). Полная история собирается на стороне сервера.
- Стриминг через SSE с воспроизведением фрагментов; desktop-оболочка
  показывает фрагменты размышлений и вызовы инструментов от агентов scepter.
- `chat.send` поддерживает идентификатор диалога, переопределение модели и
  флаги памяти (семантику шлюза памяти см. в [руководстве по Arona](./arona.md)).

## 2. Панели

Панели создаются из одного промпта: движок генерирует макет и виджеты, а
затем сохраняет их в хранилище рабочих пространств scepter
(`.amphoreus/workspace.toml` + `.noa/views/*.view.toml`). Редактирование
структурно, а не «чёрный ящик»: видны сырая привязка источника данных, список
виджетов и состояние соединения; есть диалог глубокого редактирования каждой
строки и свёрнутое окно настройки на естественном языке.

Три вида панелей:

- **Таблица данных** — табличные представления с типизированными полями,
  сортировкой/фильтрацией/группировкой; записи проходят через виды
  редактирования `table.*` с настоящей валидацией и откатом.
- **Медиа-конвейер** — граф узлов в стиле Dify (LLM-узлы, генерация
  изображений, HTTP, извлечение из знаний, ветвление); конвейеры выполняются
  на сервере с потоковым прогрессом и могут вызываться агентами как
  инструменты.
- **3D-двойник** — деревья моделей устройств с мировыми координатами,
  конфигурацией сцены и закладками камеры.

## 3. Мультиканальные интеграции

Мост каналов соединяет ядро с IM-платформами (Discord, Slack, Matrix, Lark,
…). Входящие сообщения становятся ходами чата; исходящие ответы стримятся
обратно в тот же канал. Для каждого канала требуется собственное
OAuth-приложение, одобренное для продакшн-использования.

## 4. Семантический поиск и знания

- `search.semantic` мостит к векторному поиску scepter (индекс рабочих
  пространств ApoRia + долгосрочные воспоминания PhiLia, слитые в один
  ранжированный список).
- Базы знаний (создание / добавление документов / подписка) хранятся в
  Postgres и доступны для поиска через ту же RPC-поверхность.
- Отчёты агентов индексируются автоматически, поэтому прошлые отчёты
  семантически извлекаемы.

## 5. Аутентификация

Аутентификация делегирована: scepter доверяет идентификатору пользователя,
который передаёт шлюз chest (`X-User-Id` / `user_id`); chest получает его от
Arona или из потока приглашений. Роли RBAC (admin / operator / viewer /
agent) ограничивают операции записи на панелях и в рабочих пространствах.

## Справочник переменных окружения (подмножество)

| Переменная | Назначение |
|---|---|
| `DATABASE_URL` | Postgres (обязательно) |
| `ENTELECHEIA_SCEPTER_URL` / `WS_URL` | RPC/WS конечные точки движка агентов |
| `LLM_DEFAULT_PROVIDER_ENDPOINT` / `_API_KEY` / `_MODELS` | Провайдер чат-модели (обычно Arona) |
| `BIGMODEL_API_KEY*` | Медиа-конвейер (GLM-зрение / генерация изображений CogView) |
| `CHANNEL_*` | Учётные данные IM-каналов по платформам |
