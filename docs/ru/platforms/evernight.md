# Evernight — брокер промышленных протоколов

Evernight — это промышленный край: кроссплатформенный брокер, говорящий на
полевых протоколах (Modbus, S7comm, MC Protocol, EtherNet/IP, EtherCAT, CAN,
OPC UA, MQTT, …), опрашивающий датчики, вычисляющий аварии и проталкивающий
события в стек celestia-island. Он также управляет серверами моделей на узле
(ollama / whisper / vLLM) для инференса на краю.

## Архитектура одним взглядом

```text
Поле: PLC / MCU / датчики (Modbus, S7comm, MC, EtherCAT, CAN, OPC UA, …)
        ▼
   evernight (краевой узел)
   ├─ Адаптеры протоколов: опрос → декодирование → типизированные показания
   ├─ Движок аварий: пороговые правила → события-триггеры
   ├─ Временные ряды: буферизованные показания с двойными метками времени
   ├─ Запись/воспроизведение: кольцевой буфер → сегментное хранилище → инъекция воспроизведения
   ├─ Менеджер серверов моделей: развёртывание ollama/whisper/vLLM (сначала GPU)
   └─ Northbound: JSON-RPC-триггеры через Unix-сокет → entelecheia
        │
        ▼
   scepter (агенты, промышленные рабочие процессы, подтверждение записей)
```

## 1. Полевые протоколы

Адаптеры превращают собственные чтения/записи каждого протокола в
типизированные показания и команды. Путь записи огорожен: промышленные
записи требуют проверки политик и подтверждения человеком в платформе
(OreXis + потоки подтверждения).

## 2. Сбор данных и аварии

- Циклы опроса по каждой станции с настраиваемыми периодами; сбои всплывают
  как события здоровья.
- Движок аварий вычисляет пороговые правила по показаниям и порождает
  события, маршрутизируемые по темам, в northbound-приёмник триггеров.

## 3. Время и запись

Показания несут двойные метки времени (настенные часы для отображения и
аудита, монотонные — для упорядочивания и слияния). Конвейер
записи/воспроизведения держит кольцевой буфер, сохраняет сегменты и может
внедрять воспроизведённые данные обратно в конвейер триггеров — общая
предпосылка для слоёв состояния мира и обучения.

## 4. Обслуживание моделей на краю

`model_server` управляет рантаймами моделей на узле: развёртывание моделей в
контейнерах (ollama, whisper.cpp, vLLM) с размещением сначала на GPU и
откатом на CPU — строительный блок для реактивного инференса на краю,
который никогда не зависит от онлайн-LLM.

## 5. Интеграция northbound

События текут к scepter из entelecheia через приёмник JSON-RPC-триггеров на
Unix-сокете (маршрутизация по темам); шлюз устройство↔облак регистрирует
идентичность узла и телеметрию. Всё физическое маршрутизируется через
evernight.

## Справочник переменных окружения (подмножество)

| Переменная | Назначение |
|---|---|
| `EVERNIGHT_SOCK` | Unix-сокет для триггеров и телеметрии к scepter |
| `EVERNIGHT_*` | Конфигурация подключений по протоколам |
| container/GPU env | Развёртывание серверов моделей (рантаймы ollama/vLLM) |
