# Arona — шлюз моделей, память и кластер

Arona — это управляющая плоскость платформы: шлюз моделей, менеджер рантайма
саморазвёртывания и веб-дашборд. Задача, которую она решает, — превратить
«модель, скачанную на какой-то машине» в то, что вся платформа может
маршрутизировать, учитывать и запоминать. Это руководство организовано по
возможностям: маршрутизация моделей, долгосрочная память и многоузловые
кластеры.

## Архитектура одним взглядом

```text
shittim-chest / любой OpenAI-клиент
        │  /v1/chat/completions (Bearer API key)
        ▼
   Шлюз Arona (node-2:8420)
   ├─ Роутер: алиасы → балансировка least-count между бэкендами
   ├─ Шлюз памяти: инъекция recall → чат → writeback (эпизоды)
   └─ Контрольная плоскость агентов (/ws/agent) ──► arona-agent на GPU-узлах
        │
        ▼
   Бэкенды: ollama · external (OpenAI-совместимые) · движки, развёрнутые агентами
```

Весь управляющий трафик (дашборд, агенты, память) идёт поверх WebSocket с
сообщениями JSON-RPC 2.0; единственная REST-поверхность — OpenAI-совместимые
конечные точки `/v1/*`.

## 1. Модели

### Регистрация бэкенда

Бэкенды регистрируются как `ollama` или `external` (любой OpenAI-совместимый
сервер — vLLM, TGI, LMDeploy, роутер TileRT, …):

```bash
POST /api/admin/backends        # Bearer ADMIN_TOKEN
  {"type": "ollama", "name": "node3-ollama", "url": "http://host:11434"}
  {"type": "external", "name": "my-vllm", "url": "http://host:8000",
   "api_key": "...", "models": ["qwen2.5-72b"]}
```

Зарегистрированные бэкенды сохраняются между рестартами (таблица
`backend_configs`) и непрерывно проверяются на здоровье: внешние бэкенды
остаются закрытыми (fail closed) до первого успешного зонда `/v1/models`, а
их список моделей обновляется динамически.

### Самостоятельное развёртывание модели на узле

Бинарник `arona-agent` запускается на GPU-машинах и подключается обратно к
панели. Разверните модель со страницы **Agents** дашборда (или через
`agents.deploy` с пустым `agent_id` для автоматического выбора наименее
загруженного узла). Агент скачивает модель (HuggingFace или реестр Ollama),
запускает движок (llama.cpp / vLLM / Ollama) и сообщает конечную точку
движка — панель автоматически регистрирует её как маршрутизируемый бэкенд
`agent-{model}` и удаляет его при остановке.

Адрес привязки движка: задайте `ARONA_AGENT_BIND_ADDR=0.0.0.0` на узлах,
которые должны обслуживать трафик для панели. Внимание: порты движка не
аутентифицируются — разворачивайте только в доверенных сетях.

### Привязка диалогов

Диалоги закрепляются за одним бэкендом (привязка сессии), что позволяет
переиспользовать рантаймные KV-кэши. Если закреплённый бэкенд становится
нездоровым, роутер переключается на другой и закрепляется заново.

## 2. Долгосрочная память

Arona — это **шлюз памяти**: она не обучает модели — она оркестрирует сервис
памяти (агент PhiLia из entelecheia) вокруг вашей существующей модели.

### Включение

```bash
ARONA_MEMORY_URL=ws://<scepter-host>:8424/ws
ARONA_MEMORY_TOKEN=<scepter connection token>
ARONA_MEMORY_WRITEBACK=1        # default on; 0 disables writeback
```

### Что происходит при каждом чате

1. **Извлечение (recall)** — последнее сообщение пользователя векторизуется и
   запрашивается у сервиса памяти; релевантные воспоминания внедряются как
   системная секция `## Relevant Long-Term Memories` (идемпотентно).
2. **Чат** — собранный контекст маршрутизируется модели.
3. **Обратная запись (writeback)** — завершённый ход извлекается
   эвристически (`User: … / Assistant: …`, ноль вызовов LLM) и сохраняется
   как эпизод в графе памяти (на pgvector, переживает рестарты).
4. **Состояние** — каждый ответ сообщает `memory: enabled | disabled |
   offline`; REST-поверхность добавляет заголовок `X-Arona-Memory`. Сбои
   никогда не блокируют чат; `offline` означает, что сервис памяти
   недоступен, и это всегда видно в UI.

Переопределение на вызов: `chat.send` принимает `memory: true|false`.

### Управление

Страница **Memory** дашборда показывает активность извлечения, обратной
записи и удаления и позволяет удалять сохранённые узлы. Сессии хранятся на
стороне сервера: передайте `conversation_id` в `chat.send`, и сервер соберёт
историю вместо клиента.

## 3. Операции

- **Аутентификация**: регистрация блокируется после первого бутстрапа
  администратора (`ARONA_REGISTRATION_OPEN=1` открывает её снова). Конечные
  точки администрирования требуют `ARONA_ADMIN_TOKEN`; без него они работают
  по принципу fail closed.
- **Учёт**: использование и стоимость записываются по каждому API key
  (`usage.list`, тарифные уровни с квотами и лимитами частоты).
- **Здоровье**: `/api/health` и `/v1/health` сообщают версию и хеш сборки.

## Справочник переменных окружения

| Переменная | Назначение |
|---|---|
| `DATABASE_URL` | Postgres (обязательно) |
| `JWT_SECRET` | Подпись токенов (обязательно вне mock-режима) |
| `ARONA_HOST` / `ARONA_PORT` | Адрес привязки (по умолчанию `0.0.0.0:8420`) |
| `ARONA_ADMIN_TOKEN` | Bearer-токен для `/api/admin/*` |
| `ARONA_REGISTRATION_OPEN` | Снова открыть саморегистрацию |
| `ARONA_MEMORY_URL` / `ARONA_MEMORY_TOKEN` / `ARONA_MEMORY_WRITEBACK` | Шлюз памяти |
| `ARONA_AGENT_NAME` / `ARONA_PANEL_URL` / `ARONA_AGENT_BIND_ADDR` | Узел агента |
