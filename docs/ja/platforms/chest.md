# Shittim Chest — チャット、パネル、統合

Shittim Chest はユーザーの顔です: チャット、パネル、マルチチャネル統合 ——
モデルとエージェントの能力を、毎日使うものに変えるレイヤです。モデルと
メモリは Arona から、エージェント、パネル、産業ワークフローは entelecheia
の scepter から受け取ります。このガイドは機能別に構成されています:
チャット、パネル、チャネル、検索、認証。

## アーキテクチャの概観

```text
あなた
 │  デスクトップアプリ · Web UI · 12+ の IM チャネル (discord/slack/matrix/lark/…)
 ▼
Shittim Chest コア (node-2:8425)
 ├─ チャット: 会話は Postgres に永続化、SSE/WS でストリーミング
 ├─ パネル: データグリッド / メディアパイプライン / 3D ツイン (パネルエンジン)
 ├─ メディアパイプライン: LLM ビジョンレビュー、画像生成、3D 生成 (スタブ)
 ├─ チャネルブリッジ: 双方向 IM ↔ コア (webhooks + WS)
 └─ RPC ブリッジ → scepter (エージェント、セマンティック検索、ナレッジベース)
        │
        ▼
Arona (モデル、メモリ) · scepter (エージェント、パネル、検索)
```

## 1. チャット

- 会話とメッセージは Postgres に永続化されます（`conversations` /
  `messages` テーブル）。履歴全体はサーバー側で組み立てられます。
- チャンクリプレイ付きの SSE によるストリーミング。デスクトップシェルは
  scepter エージェントの思考チャンクとツール呼び出しを表示します。
- `chat.send` は会話 ID、モデルオーバーライド、メモリフラグをサポート
  します（メモリゲートウェイのセマンティクスは
  [Arona ガイド](./arona.md) を参照）。

## 2. パネル

パネルは 1 つのプロンプトから作成されます: エンジンがレイアウトと
ウィジェットを生成し、scepter のワークスペースストア
（`.amphoreus/workspace.toml` + `.noa/views/*.view.toml`）へ永続化します。
編集は構造的で、ブラックボックスではありません: 生のデータソース
バインディング、ウィジェット一覧、接続状態が可視であり、行ごとの詳細編集
ダイアログと折りたたみ式の自然言語チューニングボックスがあります。

3 種類のパネルがあります:

- **データグリッド** —— 型付きフィールドのテーブルビュー。ソート/フィルタ/
  グループ化。書き込みは実際の検証とロールバック付きの `table.*` 編集
  種別を通ります。
- **メディアパイプライン** —— Dify 風のノードグラフ（LLM ノード、画像生成、
  HTTP、ナレッジ検索、分岐）。パイプラインはストリーミング進捗付きで
  サーバー側で実行され、エージェントからツールとして呼び出せます。
- **3D ツイン** —— ワールド座標付きのデバイスモデルツリー、シーン設定、
  カメラブックマーク。

## 3. マルチチャネル統合

チャネルブリッジはコアを IM プラットフォーム（Discord、Slack、Matrix、
Lark、…）へ接続します。受信メッセージはチャットターンになり、送信
レスポンスは同じチャネルへストリーミングし戻ります。各チャネルには、
本番利用が承認された OAuth アプリケーションが必要です。

## 4. セマンティック検索とナレッジ

- `search.semantic` は scepter のベクトル検索へブリッジします（ApoRia
  ワークスペースインデックス + PhiLia 長期メモリが 1 つのランキング
  リストに融合）。
- ナレッジベース（作成 / ドキュメント追加 / 購読）は Postgres に永続化
  され、同じ RPC サーフェスから検索できます。
- エージェントレポートは自動的にインデックスされ、過去のレポートが
  セマンティックに検索できます。

## 5. 認証

認証は委任されています: scepter は chest ゲートウェイが供給する呼び出し元
ユーザー ID（`X-User-Id` / `user_id`）を信頼します。この ID は chest が
Arona または招待フローから取得します。RBAC ロール（admin / operator /
viewer / agent）がパネルとワークスペースへの書き込み操作をゲートします。

## 環境変数リファレンス（抜粋）

| 変数 | 用途 |
|---|---|
| `DATABASE_URL` | Postgres（必須） |
| `ENTELECHEIA_SCEPTER_URL` / `WS_URL` | エージェントエンジンの RPC/WS エンドポイント |
| `LLM_DEFAULT_PROVIDER_ENDPOINT` / `_API_KEY` / `_MODELS` | チャットモデルプロバイダ（通常は Arona） |
| `BIGMODEL_API_KEY*` | メディアパイプライン（GLM ビジョン / CogView 画像生成） |
| `CHANNEL_*` | プラットフォームごとの IM チャネル資格情報 |
