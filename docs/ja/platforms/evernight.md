# Evernight — 産業プロトコルブローカー

Evernight は産業エッジです: フィールドプロトコル（Modbus、S7comm、
MC Protocol、EtherNet/IP、EtherCAT、CAN、OPC UA、MQTT、…）を話す
クロスプラットフォームのブローカーであり、センサーをポーリングし、
アラームを評価し、イベントを celestia-island スタックへプッシュします。
エッジ推論のためのノード上モデルサーバー（ollama / whisper / vLLM）の
管理も行います。

## アーキテクチャの概観

```text
フィールド: PLC / MCU / センサー (Modbus, S7comm, MC, EtherCAT, CAN, OPC UA, …)
        ▼
   evernight (エッジノード)
   ├─ プロトコルアダプタ: ポーリング → デコード → 型付き読み取り値
   ├─ アラームエンジン: しきい値ルール → トリガーイベント
   ├─ 時系列: 二重タイムスタンプ付きのバッファ読み取り値
   ├─ 録画/リプレイ: リングバッファ → 分割ストレージ → リプレイ注入
   ├─ モデルサーバーマネージャ: ollama/whisper/vLLM をデプロイ (GPU 優先)
   └─ ノースバウンド: Unix ソケット JSON-RPC トリガー → entelecheia
        │
        ▼
   scepter (エージェント、産業ワークフロー、書き込み承認)
```

## 1. フィールドプロトコル

アダプタは各プロトコル固有の読み書きを、型付きの読み取り値とコマンドへ
変換します。書き込みパスはゲートされています: 産業への書き込みには、
プラットフォームでのポリシー検証と人間の承認が必要です（OreXis +
承認フロー）。

## 2. センシングとアラーム

- ステーションごとのポーリングループ（周期は設定可能）。失敗はヘルス
  イベントとして表面化します。
- アラームエンジンは読み取り値に対してしきい値ルールを評価し、トピック
  ルーティングされたイベントをノースバウンドのトリガーシンクへ送出します。

## 3. 時刻と録画

読み取り値は二重のタイムスタンプを運びます（表示/監査用の壁時計、
順序付け/融合用の単調時計）。録画/リプレイパイプラインはリングバッファを
保持し、セグメントを永続化し、リプレイデータをトリガーパイプラインへ
再注入できます —— 世界状態レイヤと学習レイヤの共有前提です。

## 4. エッジモデルサービング

`model_server` はノード上のモデルランタイムを管理します: コンテナ上の
モデルデプロイ（ollama、whisper.cpp、vLLM）を GPU 優先・CPU フォールバック
の配置で行います —— オンラインの LLM に決して依存しない、リアクティブな
エッジ推論の構成要素です。

## 5. ノースバウンド統合

イベントは Unix ソケット JSON-RPC のトリガーシンク（トピックルーティング）
を介して entelecheia の scepter へ流れます。デバイス↔クラウドゲートウェイが
ノードのアイデンティティとテレメトリを登録します。物理的なものはすべて
evernight を通ってルーティングされます。

## 環境変数リファレンス（抜粋）

| 変数 | 用途 |
|---|---|
| `EVERNIGHT_SOCK` | scepter へのトリガー/テレメトリ用 Unix ソケット |
| `EVERNIGHT_*` | プロトコルごとの接続設定 |
| container/GPU env | モデルサーバーデプロイ（ollama/vLLM ランタイム） |
