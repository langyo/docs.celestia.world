# Shittim Chest — المحادثة واللوحات والتكاملات

Shittim Chest هو وجه المستخدم: المحادثة واللوحات والتكاملات متعددة القنوات —
الطبقة التي تحوّل قدرات النماذج والوكلاء إلى شيء تستخدمه كل يوم. يأخذ
النماذج والذاكرة من Arona، والوكلاء واللوحات وتدفقات العمل الصناعية من
scepter في entelecheia. هذا الدليل مرتّب حسب القدرة: المحادثة، واللوحات،
والقنوات، والبحث، والمصادقة.

## البنية في لمحة

```text
You
 │  desktop app · web UI · 12+ IM channels (discord/slack/matrix/lark/…)
 ▼
Shittim Chest core (node-2:8425)
 ├─ Chat: conversations persisted in Postgres, streamed SSE/WS
 ├─ Panels: data grid / media pipeline / 3D twin (panel engine)
 ├─ Media pipeline: LLM vision review, image generation, 3D gen (stub)
 ├─ Channel bridge: bidirectional IM ↔ core (webhooks + WS)
 └─ RPC bridge → scepter (agents, semantic search, knowledge base)
        │
        ▼
Arona (models, memory) · scepter (agents, panels, search)
```

## 1. المحادثة

- المحادثات والرسائل تُحفظ في Postgres (جدولا `conversations` و`messages`).
  يُجمَّع التاريخ الكامل على الخادم.
- البث عبر SSE مع إعادة تشغيل الأجزاء؛ ويعرض هيكل سطح المكتب أجزاء التفكير
  واستدعاءات الأدوات من وكلاء scepter.
- يدعم `chat.send` معرّف المحادثة، وتجاوز النموذج، وأعلام الذاكرة (راجع
  [دليل Arona](./arona.md) لدلالات بوابة الذاكرة).

## 2. اللوحات

تُنشأ اللوحات من موجّه واحد: يولّد المحرك التخطيط والمكونات ثم يحفظها في
تخزين مساحة عمل scepter (`.amphoreus/workspace.toml` +
`.noa/views/*.view.toml`). التحرير بنيوي لا صندوق أسود: ارتباط مصدر
البيانات الخام، وقائمة المكونات، وحالة الاتصال — كلها مرئية، مع حوار تحرير
عميق لكل صف وصندوق ضبط باللغة الطبيعية مطوي.

ثلاثة أنواع من اللوحات:

- **شبكة البيانات** — عروض جدولية بحقول مُنمّطة، وفرز/تصفية/تجميع؛ وتمرّ
  عمليات الكتابة بأنواع تحرير `table.*` مع تحقق حقيقي وتراجع.
- **خط أنابيب الوسائط** — مخطط عُقد على طراز Dify (عُقد LLM، وتوليد الصور،
  وHTTP، واسترجاع المعرفة، والتفريع)؛ تعمل الخطوط من الخادم مع تقدّم متدفق
  ويمكن للوكلاء استدعاؤها أدواتٍ.
- **التوأم ثلاثي الأبعاد** — أشجار نماذج أجهزة بإحداثيات عالمية، وضبط
  للمشهد، وإشارات مرجعية للكاميرا.

## 3. التكاملات متعددة القنوات

يربط جسر القنوات النواة بمنصات المراسلة الفورية (Discord وSlack وMatrix
وLark و…). الرسائل الواردة تصبح أدوار محادثة؛ والاستجابات الصادرة تعود
متدفقةً عبر القناة نفسها. تتطلب كل قناة اعتماد تطبيق OAuth الخاص بها
للاستخدام الإنتاجي.

## 4. البحث الدلالي والمعرفة

- يجسّر `search.semantic` إلى البحث المتجهي في scepter (فهرس مساحة عمل
  ApoRia + ذكريات PhiLia طويلة الأمد تُدمج في قائمة مرتّبة واحدة).
- قواعد المعرفة (إنشاء / إضافة مستندات / اشتراك) تُحفظ في Postgres وتقبل
  البحث عبر سطح RPC نفسه.
- تُفهرس تقارير الوكلاء تلقائيًا، فتكون التقارير السابقة قابلة للاسترجاع
  دلاليًا.

## 5. المصادقة

المصادقة مفوَّضة: يثق scepter بمعرّف المستخدم المتقدم الذي تزوّده به بوابة
chest (`X-User-Id` / `user_id`)، وهو ما يحصل عليه chest من Arona أو من تدفق
الدعوات. تقيّد أدوار RBAC (admin / operator / viewer / agent) عمليات
الكتابة على اللوحات ومساحات العمل.

## مرجع متغيرات البيئة (مختصر)

| المتغير | الغرض |
|---|---|
| `DATABASE_URL` | Postgres (مطلوب) |
| `ENTELECHEIA_SCEPTER_URL` / `WS_URL` | نقاط نهاية RPC/WS لبيئة تشغيل الوكلاء |
| `LLM_DEFAULT_PROVIDER_ENDPOINT` / `_API_KEY` / `_MODELS` | مزوّد نموذج المحادثة (عادةً Arona) |
| `BIGMODEL_API_KEY*` | خط أنابيب الوسائط (رؤية GLM / توليد صور CogView) |
| `CHANNEL_*` | بيانات اعتماد قنوات المراسلة لكل منصة |
