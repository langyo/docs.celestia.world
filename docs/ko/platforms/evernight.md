# Evernight — 산업 프로토콜 브로커

Evernight는 산업 엣지입니다: 필드 프로토콜(Modbus, S7comm, MC Protocol,
EtherNet/IP, EtherCAT, CAN, OPC UA, MQTT, …)을 사용해 센서를 폴링하고,
알람을 평가하고, 이벤트를 celestia-island 스택으로 푸시하는 크로스 플랫폼
브로커입니다. 엣지 추론을 위해 노드 상의 모델 서버(ollama / whisper /
vLLM)도 관리합니다.

## 한눈에 보는 아키텍처

```text
필드: PLC / MCU / 센서 (Modbus, S7comm, MC, EtherCAT, CAN, OPC UA, …)
        ▼
   evernight (엣지 노드)
   ├─ 프로토콜 어댑터: 폴링 → 디코딩 → 타입화된 판독값
   ├─ 알람 엔진: 임계값 규칙 → 트리거 이벤트
   ├─ 시계열: 이중 타임스탬프가 붙은 버퍼링된 판독값
   ├─ 녹화/재생: 링 버퍼 → 분할 저장 → 재생 주입
   ├─ 모델 서버 매니저: ollama/whisper/vLLM 배포 (GPU 우선)
   └─ 노스바운드: Unix 소켓 JSON-RPC 트리거 → entelecheia
        │
        ▼
   scepter (에이전트, 산업 워크플로, 쓰기 승인)
```

## 1. 필드 프로토콜

어댑터는 각 프로토콜의 고유 읽기/쓰기를 타입화된 판독값과 명령으로
변환합니다. 쓰기 경로는 게이트되어 있습니다: 산업 쓰기는 플랫폼에서 정책
검증과 인간 승인을 요구합니다(OreXis + 승인 플로우).

## 2. 센싱 및 알람

- 스테이션별 폴링 루프가 설정 가능한 주기로 실행됩니다; 실패는 헬스
  이벤트로 표면화됩니다.
- 알람 엔진은 판독값에 대해 임계값 규칙을 평가하고 토픽 라우팅된 이벤트를
  노스바운드 트리거 싱크로 내보냅니다.

## 3. 시간과 녹화

판독값은 이중 타임스탬프를 가집니다(표시/감사용 벽시계 시각, 정렬/융합용
단조 시각). 녹화/재생 파이프라인은 링 버퍼를 유지하고, 세그먼트를 저장하며,
재생된 데이터를 트리거 파이프라인에 다시 주입할 수 있습니다 — 월드 상태
및 학습 계층의 공유 전제 조건입니다.

## 4. 엣지 모델 서빙

`model_server`는 노드에서 모델 런타임을 관리합니다: GPU 우선, CPU 폴백
배치로 컨테이너에서 모델을 배포합니다(ollama, whisper.cpp, vLLM) — 온라인
LLM에 절대 의존하지 않는 반응형 엣지 추론의 빌딩 블록입니다.

## 5. 노스바운드 통합

이벤트는 Unix 소켓 JSON-RPC 트리거 싱크(토픽 라우팅)를 통해 entelecheia의
scepter로 흐릅니다; 장치↔클라우드 게이트웨이가 노드 신원과 텔레메트리를
등록합니다. 모든 물리적 대상은 evernight를 거칩니다.

## 환경 변수 참조 (일부)

| 변수 | 용도 |
|---|---|
| `EVERNIGHT_SOCK` | scepter로 가는 트리거/텔레메트리용 Unix 소켓 |
| `EVERNIGHT_*` | 프로토콜별 연결 구성 |
| 컨테이너/GPU 환경변수 | 모델 서버 배포 (ollama/vLLM 런타임) |
