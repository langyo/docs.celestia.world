# Shittim Chest — 채팅, 패널 및 통합

Shittim Chest는 사용자 접점입니다: 채팅, 패널, 다중 채널 통합 — 모델과
에이전트 기능을 매일 사용하는 무언가로 바꾸는 계층입니다. 모델과 메모리는
Arona에서, 에이전트·패널·산업 워크플로는 entelecheia의 scepter에서
가져옵니다. 이 가이드는 기능별로 구성되어 있습니다: 채팅, 패널, 채널, 검색,
인증.

## 한눈에 보는 아키텍처

```text
사용자
 │  데스크톱 앱 · 웹 UI · 12+ IM 채널 (discord/slack/matrix/lark/…)
 ▼
Shittim Chest 코어 (node-2:8425)
 ├─ 채팅: Postgres에 유지되는 대화, SSE/WS 스트리밍
 ├─ 패널: 데이터 그리드 / 미디어 파이프라인 / 3D 트윈 (패널 엔진)
 ├─ 미디어 파이프라인: LLM 비전 검토, 이미지 생성, 3D 생성 (스텁)
 ├─ 채널 브리지: 양방향 IM ↔ 코어 (웹훅 + WS)
 └─ RPC 브리지 → scepter (에이전트, 시맨틱 검색, 지식 베이스)
        │
        ▼
Arona (모델, 메모리) · scepter (에이전트, 패널, 검색)
```

## 1. 채팅

- 대화와 메시지는 Postgres에 유지됩니다(`conversations` / `messages`
  테이블). 전체 이력은 서버 쪽에서 조립됩니다.
- 청크 리플레이가 있는 SSE로 스트리밍됩니다; 데스크톱 셸은 thinking 청크와
  scepter 에이전트의 도구 호출을 표시합니다.
- `chat.send`는 대화 id, 모델 재정의, 메모리 플래그를 지원합니다(메모리
  게이트웨이 의미론은 [Arona 가이드](./arona.md)를 참고하세요).

## 2. 패널

패널은 하나의 프롬프트에서 만들어집니다: 엔진이 레이아웃과 위젯을 생성한
다음 scepter의 워크스페이스 저장소(`.amphoreus/workspace.toml` +
`.noa/views/*.view.toml`)에 유지합니다. 편집은 구조화되어 있지 블랙박스가
아닙니다: 원시 데이터 소스 바인딩, 위젯 목록, 연결 상태가 보이고, 행별
심층 편집 대화상자와 접힌 자연어 튜닝 박스가 있습니다.

세 가지 패널 종류:

- **데이터 그리드** — 타입화된 필드가 있는 테이블 뷰, 정렬/필터/그룹화,
  쓰기는 실제 검증과 롤백이 있는 `table.*` 편집 종류를 거칩니다.
- **미디어 파이프라인** — Dify 스타일의 노드 그래프(LLM 노드, 이미지
  생성, HTTP, 지식 검색, 분기); 파이프라인은 스트리밍 진행 표시와 함께
  서버 쪽에서 실행되며 에이전트가 도구로 호출할 수 있습니다.
- **3D 트윈** — 월드 좌표가 있는 장치 모델 트리, 장면 구성 및 카메라
  북마크.

## 3. 다중 채널 통합

채널 브리지는 코어를 IM 플랫폼(Discord, Slack, Matrix, Lark, …)에
연결합니다. 인바운드 메시지는 채팅 턴이 되고; 아웃바운드 응답은 같은 채널로
스트리밍되어 돌아갑니다. 각 채널은 프로덕션 사용이 승인된 자체 OAuth
애플리케이션을 요구합니다.

## 4. 시맨틱 검색 및 지식

- `search.semantic`은 scepter의 벡터 검색으로 연결됩니다(ApoRia
  워크스페이스 인덱스 + PhiLia 장기 메모리가 하나의 랭킹 목록으로
  융합됨).
- 지식 베이스(생성 / 문서 추가 / 구독)는 Postgres에 유지되며 같은 RPC
  표면을 통해 검색됩니다.
- 에이전트 보고서는 자동으로 인덱싱되므로 과거 보고서를 시맨틱하게 다시
  찾을 수 있습니다.

## 5. 인증

인증은 위임됩니다: scepter는 chest 게이트웨이가 전달하는 호출자 사용자
ID(`X-User-Id` / `user_id`)를 신뢰하며, chest는 이를 Arona 또는 초대
플로우에서 얻습니다. RBAC 역할(admin / operator / viewer / agent)이 패널과
워크스페이스의 쓰기 작업을 제어합니다.

## 환경 변수 참조 (일부)

| 변수 | 용도 |
|---|---|
| `DATABASE_URL` | Postgres (필수) |
| `ENTELECHEIA_SCEPTER_URL` / `WS_URL` | 에이전트 엔진 RPC/WS 엔드포인트 |
| `LLM_DEFAULT_PROVIDER_ENDPOINT` / `_API_KEY` / `_MODELS` | 채팅 모델 프로바이더 (보통 Arona) |
| `BIGMODEL_API_KEY*` | 미디어 파이프라인 (GLM 비전 / CogView 이미지 생성) |
| `CHANNEL_*` | 플랫폼별 IM 채널 자격 증명 |
